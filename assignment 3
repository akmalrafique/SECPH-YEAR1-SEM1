#include<iostream>
#include<fstream>
#include<iomanip>
using namespace std;
int main(){
const int size=20
char student[size];
int intake[size],enrollement[size],output[size];
int totalintake=0,totalenroll=0,totaloutput=0;
getInput(.......);
totalintake=calTotal(intake,size);
totalenroll=calTotal(enrollement,size);
totaloutput=calTotal(output,size);
index=getLowest(student,intake,size);
index=getLowest(student,enrollment,size);
index=getLowest(student,output,size);
}
void getInput(STUDENT[],INTAKE[],ENROLLMENT[],OUTPUT[]){
int i=0;
ifstream infile("input.txt");
if(!infile){
cout<<"fail to open the file";
return 0;
}
while(infile>>STUDENT[i]>>INTAKE[i]>>ENROLLMENT[i]>>OUTPUT[i]){
i++;
}
}
int calTotal( int total[],int SIZE){
int totall=0;
for(int a=0;a<SIZE;a++){
totall+=total[a];
}
return totall;
}
int getLowest(char STUDENT[],int ARRAY[],int SIZE){
int lowest=-1;
while(int i=0;i<SIZE;i++){

}




}
void getHighest(){}

